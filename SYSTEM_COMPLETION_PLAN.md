# خطة إكمال النظام المحاسبي

تاريخ التخطيط: 2026-01-11

---

## ✅ ما تم إنجازه

1. ✅ **جداول قاعدة البيانات**: `payment_vouchers`, `payment_voucher_operations`
2. ✅ **API Routes**: `/api/payments` مع جميع عمليات CRUD
3. ✅ **ربط Frontend مع Backend**: سند الصرف (payment voucher) يعمل
4. ✅ **تحديث أرصدة الصناديق**: يعمل تلقائياً
5. ✅ **تحميل السندات**: من قاعدة البيانات

---

## ❌ ما يحتاج لإكمال

### الأولوية العالية 🔴

1. **ربط سند القبض (Receipt Voucher) مع API**
   - سند القبض الحالي يستخدم `handleSubmit` بسيط (state محلي)
   - يحتاج ربط مع API مثل سند الصرف

2. **ربط السندات بالقيد المحاسبي (Journal Entry)**
   - عند حفظ سند صرف/قبض، يجب إنشاء قيد محاسبي تلقائياً
   - ربط السندات بقيود اليومية

### الأولوية المتوسطة 🟡

3. **وظائف التعديل والحذف من الواجهة**
   - إضافة أزرار التعديل والحذف في الجدول
   - ربطها مع API

4. **تطوير التقارير**
   - تقرير سندات الصرف/القبض
   - تقرير التدفق النقدي

### الأولوية المنخفضة 🟢

5. **الطباعة**
   - طباعة سند الصرف
   - طباعة سند القبض

---

## 📋 خطة التنفيذ

### المرحلة 1: إكمال سند القبض (أولوية عالية)
- تحديث `handleSubmit` لسند القبض لاستخدام API
- دعم جميع الحقول (cashBox, party, amount, currency, exchangeRate, reference)

### المرحلة 2: ربط السندات بالقيد المحاسبي (أولوية عالية)
- إضافة `journalEntryId` إلى جدول `payment_vouchers`
- عند إنشاء سند، إنشاء قيد محاسبي تلقائياً:
  - سند صرف: مدين (حساب المصروف/الدائن)، دائن (الصندوق)
  - سند قبض: مدين (الصندوق)، دائن (حساب الإيرادات/العميل)

### المرحلة 3: وظائف التعديل والحذف (أولوية متوسطة)
- إضافة زر "تعديل" في الجدول
- إضافة زر "حذف" في الجدول
- ربطها مع API

---

**الحالة**: جاهز للتنفيذ
