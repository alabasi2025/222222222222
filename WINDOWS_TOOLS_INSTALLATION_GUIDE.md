# دليل تثبيت الأدوات والبرامج في Windows

تاريخ الإعداد: 2026-01-11

---

## 📊 حالة الأدوات الحالية

### ✅ الأدوات المثبتة (مؤكدة):
- ✅ **Node.js**: v22.17.1
- ✅ **pnpm**: 10.4.1
- ✅ **Git**: 2.52.0.windows.1
- ✅ **PostgreSQL**: مثبت في `C:\Program Files\PostgreSQL`

### ❌ الأدوات المفقودة (تحتاج تثبيت):
- ❌ **pgAdmin**: غير موجود
- ❌ **VS Code**: غير موجود في PATH
- ❌ **ESLint Config**: يحتاج إعداد
- ❌ **Husky**: يحتاج init
- ❌ **أدوات إضافية**: كثيرة (انظر أدناه)

---

## 🎯 خطة التثبيت المرحلية

### المرحلة 1: أساسية (أولوية عالية جداً) 🔴

#### 1.1 إعداد ESLint (فحص جودة الكود)

**الحالة**: ❌ ملف `.eslintrc.json` موجود لكن يحتاج تحديث

**الإجراء**:
```bash
# تحديث ESLint config
```

**الفوائد**:
- اكتشاف الأخطاء قبل التنفيذ
- الحفاظ على أسلوب كود موحد
- تحسين جودة الكود

---

#### 1.2 إعداد Husky + lint-staged (Git Hooks)

**الحالة**: ❌ موجود في package.json لكن يحتاج init

**الإجراء**:
```bash
pnpm add -D husky lint-staged
npx husky init
```

**الفوائد**:
- فحص الكود تلقائياً قبل commit
- منع push للكود المعطوب
- تنسيق الكود تلقائياً

---

#### 1.3 تثبيت pgAdmin (أداة قاعدة البيانات)

**الحالة**: ❌ غير مثبت

**الإجراء**: 
- تنزيل من: https://www.pgadmin.org/download/pgadmin-4-windows/
- تثبيت الإصدار الأحدث

**الفوائد**:
- إدارة قاعدة البيانات بسهولة
- عرض الجداول والبيانات
- تنفيذ استعلامات SQL
- إدارة المستخدمين والصلاحيات

---

#### 1.4 إعداد VS Code (إذا لم يكن مثبت)

**الحالة**: ⚠️ غير موجود في PATH

**الإجراء**:
- تنزيل من: https://code.visualstudio.com/
- تثبيت وإضافة إلى PATH

**الفوائد**:
- IDE احترافي للتطوير
- دعم ممتاز لـ TypeScript و React
- إضافات متعددة

---

### المرحلة 2: مهمة (أولوية عالية) 🟡

#### 2.1 تثبيت Postman (اختبار API)

**الحالة**: ❌ غير مثبت

**الإجراء**:
- تنزيل من: https://www.postman.com/downloads/
- تثبيت الإصدار Desktop

**الفوائد**:
- اختبار جميع API endpoints
- حفظ الطلبات للاستخدام المستقبلي
- اختبارات تلقائية
- توثيق API

**بديل**: Thunder Client (إضافة VS Code)

---

#### 2.2 تثبيت DBeaver (أداة قاعدة بيانات متقدمة)

**الحالة**: ❌ غير مثبت

**الإجراء**:
- تنزيل من: https://dbeaver.io/download/
- تثبيت Community Edition (مجاني)

**الفوائد**:
- واجهة احترافية
- دعم عدة قواعد بيانات
- تصدير/استيراد البيانات
- تحرير البيانات مباشرة

---

#### 2.3 إضافة أدوات الأمان

**الحالة**: ⚠️ بعضها موجود في package.json لكن يحتاج إعداد

**الإجراء**:
```bash
# التحقق من التثبيت
pnpm list helmet cors express-rate-limit compression winston morgan dotenv
```

**الأدوات**:
- ✅ **Helmet**: موجود (أمان Express)
- ✅ **CORS**: موجود (إدارة Cross-Origin)
- ✅ **express-rate-limit**: موجود (حماية من DDoS)
- ✅ **compression**: موجود (ضغط الاستجابات)
- ✅ **winston**: موجود (نظام تسجيل)
- ✅ **morgan**: موجود (HTTP logger)
- ✅ **dotenv**: موجود (إدارة المتغيرات)

---

### المرحلة 3: تحسينات (أولوية متوسطة) 🟢

#### 3.1 تثبيت PM2 (Process Manager للإنتاج)

**الحالة**: ❌ غير مثبت

**الإجراء**:
```bash
npm install -g pm2
```

**الفوائد**:
- إدارة العمليات
- إعادة التشغيل التلقائي
- مراقبة الأداء

---

#### 3.2 تثبيت Docker (اختياري)

**الحالة**: ❌ غير مثبت

**الإجراء**:
- تنزيل Docker Desktop من: https://www.docker.com/products/docker-desktop

**الفوائد**:
- بيئة موحدة للتطوير
- سهولة النشر
- عزل التطبيقات

---

#### 3.3 Bundle Analyzer

**الحالة**: ❌ غير مثبت

**الإجراء**:
```bash
pnpm add -D vite-bundle-visualizer
```

**الفوائد**:
- معرفة حجم كل مكتبة
- تحسين أداء التطبيق

---

## 🛠️ إضافات VS Code المهمة

### أساسية (يُنصح بشدة):

1. **ESLint** - `dbaeumer.vscode-eslint`
   - فحص الكود تلقائياً
   - إصلاح الأخطاء تلقائياً

2. **Prettier** - `esbenp.prettier-vscode`
   - تنسيق الكود تلقائياً
   - دعم جميع اللغات

3. **Thunder Client** - `rangav.vscode-thunder-client`
   - اختبار API داخل VS Code
   - بديل لـ Postman

4. **PostgreSQL** - `ckolkman.vscode-postgres`
   - دعم PostgreSQL داخل VS Code
   - تنفيذ استعلامات SQL

5. **GitLens** - `eamodio.gitlens`
   - أدوات Git محسنة
   - عرض blame و history

6. **Error Lens** - `usernamehw.errorlens`
   - عرض الأخطاء مباشرة في الكود
   - توفير الوقت في البحث

7. **Auto Rename Tag** - `formulahendry.auto-rename-tag`
   - إعادة تسمية tags تلقائياً
   - مفيد لـ JSX/TSX

8. **Path Intellisense** - `christian-kohler.path-intellisense`
   - استكمال المسارات تلقائياً
   - توفير الوقت في كتابة المسارات

9. **Import Cost** - `wix.vscode-import-cost`
   - عرض حجم الـ imports
   - تحسين الأداء

10. **Better Comments** - `aaron-bond.better-comments`
    - تعليقات ملونة
    - تنظيم الكود

11. **Code Spell Checker** - `streetsidesoftware.code-spell-checker`
    - فحص الإملاء
    - دعم اللغة العربية (اختياري)

12. **TypeScript Vue Plugin (Volar)** - `vue.volar`
    - دعم TypeScript ممتاز
    - IntelliSense محسّن

---

## 📝 إصلاح المشاكل الموجودة

### 1. مشكلة ESLint Config

**المشكلة**: ESLint يحتاج ملف config حديث

**الحل**: إنشاء `eslint.config.js` (ESLint v9)

---

### 2. مشكلة Husky

**المشكلة**: Husky موجود لكن لم يتم init

**الحل**: 
```bash
npx husky init
```

---

### 3. مشكلة VS Code PATH

**المشكلة**: VS Code غير موجود في PATH

**الحل**:
1. فتح VS Code
2. اضغط `Ctrl+Shift+P`
3. اكتب: `Shell Command: Install 'code' command in PATH`
4. اضغط Enter

---

### 4. مشكلة PostgreSQL PATH

**المشكلة**: قد تكون أدوات PostgreSQL غير موجودة في PATH

**الحل**:
- إضافة إلى PATH:
  - `C:\Program Files\PostgreSQL\18\bin` (أو رقم الإصدار)

---

## 🚀 سكريبت تثبيت سريع

### PowerShell Script لتثبيت كل شيء:

انظر ملف: `install-windows-tools.ps1`

---

## 📊 جدول الأولويات

| الأداة | الأولوية | الحالة | الإجراء |
|--------|----------|--------|---------|
| ESLint Config | 🔴 عالية جداً | ❌ | تحديث config |
| Husky + lint-staged | 🔴 عالية جداً | ⚠️ | init |
| pgAdmin | 🔴 عالية جداً | ❌ | تثبيت |
| VS Code | 🔴 عالية جداً | ⚠️ | إضافة إلى PATH |
| Postman | 🟡 عالية | ❌ | تثبيت |
| DBeaver | 🟡 عالية | ❌ | تثبيت |
| PM2 | 🟢 متوسطة | ❌ | تثبيت |
| Docker | 🟢 متوسطة | ❌ | تثبيت |
| Bundle Analyzer | 🟢 متوسطة | ❌ | تثبيت |

---

## 📚 روابط التثبيت

### برامج Windows:
- **PostgreSQL 18**: https://www.postgresql.org/download/windows/
- **pgAdmin**: https://www.pgadmin.org/download/pgadmin-4-windows/
- **VS Code**: https://code.visualstudio.com/
- **Postman**: https://www.postman.com/downloads/
- **DBeaver**: https://dbeaver.io/download/
- **Docker Desktop**: https://www.docker.com/products/docker-desktop
- **Node.js** (محدث): https://nodejs.org/

### إضافات VS Code:
- **ESLint**: `ext install dbaeumer.vscode-eslint`
- **Prettier**: `ext install esbenp.prettier-vscode`
- **Thunder Client**: `ext install rangav.vscode-thunder-client`
- **PostgreSQL**: `ext install ckolkman.vscode-postgres`
- **GitLens**: `ext install eamodio.gitlens`

---

## ✅ قائمة فحص (Checklist)

### أساسية:
- [ ] إعداد ESLint Config
- [ ] تهيئة Husky
- [ ] تثبيت pgAdmin
- [ ] إضافة VS Code إلى PATH
- [ ] تثبيت إضافات VS Code الأساسية

### مهمة:
- [ ] تثبيت Postman أو Thunder Client
- [ ] تثبيت DBeaver
- [ ] التحقق من أدوات الأمان

### اختيارية:
- [ ] تثبيت PM2
- [ ] تثبيت Docker
- [ ] تثبيت Bundle Analyzer

---

## 📞 ملاحظات مهمة

1. **بعد تثبيت أي أداة**: أعد تشغيل Terminal/VS Code
2. **PostgreSQL**: تأكد من إضافة bin إلى PATH
3. **VS Code**: قد يحتاج إعادة تشغيل بعد تثبيت الإضافات
4. **PM2**: يحتاج Node.js مثبت عالمياً
5. **Docker**: يحتاج Windows 10/11 Pro أو Enterprise

---

**آخر تحديث**: 2026-01-11
